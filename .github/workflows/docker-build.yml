name: Docker Build

on:
  push:
    branches: [ main, "claude/**" ]
    paths: [ 'Dockerfile', 'docker-compose.deepseek.yml', 'infra/**', 'composer.json', 'composer.lock', 'package.json', '.dockerignore', '.github/workflows/docker-build.yml' ]
  pull_request:
    paths: [ 'Dockerfile', 'docker-compose.deepseek.yml', 'infra/**', 'composer.json', 'composer.lock', 'package.json' ]
  workflow_dispatch:

jobs:
  build-image:
    name: Build life-mdg-erp image
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build image (no push)
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: false
          tags: life-mdg-erp:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Validate DeepSeek compose file
        # Syntax/interpolation check only — does not pull the Ollama image or
        # the DeepSeek model (too slow/heavy to run on every push).
        run: docker compose -f docker-compose.deepseek.yml config
