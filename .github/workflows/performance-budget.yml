name: Performance Budget Check

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  performance-budget:
    name: Check Performance Budgets
    runs-on: ubuntu-latest
    timeout-minutes: 30
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        app: [webapp]

    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84c5d73d06cf23b74
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: '${{ matrix.app }}/package-lock.json'

      - name: Install dependencies (${{ matrix.app }})
        run: cd ${{ matrix.app }} && npm ci --legacy-peer-deps

      - name: Build frontend (${{ matrix.app }})
        run: cd ${{ matrix.app }} && npm run build
        env:
          VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN || '' }}

      - name: Check JavaScript bundle size (${{ matrix.app }})
        run: |
          cd ${{ matrix.app }}
          echo "📦 JavaScript Bundle Size Analysis (${{ matrix.app }})"
          echo "=================================================="

          VENDOR_BUNDLE=$(find dist -name 'vendor-core-*.js' -type f 2>/dev/null | head -1)
          INDEX_BUNDLE=$(find dist -name 'index-*.js' -type f 2>/dev/null | head -1)
          BUNDLE_FILE=${VENDOR_BUNDLE:-$INDEX_BUNDLE}

          if [ -z "$BUNDLE_FILE" ]; then
            echo "⚠️  Could not find main bundle file, skipping size check"
            exit 0
          fi

          SIZE_BYTES=$(stat -f%z "$BUNDLE_FILE" 2>/dev/null || stat -c%s "$BUNDLE_FILE" 2>/dev/null)
          SIZE_KB=$(awk -v b="$SIZE_BYTES" 'BEGIN {printf "%.2f", b / 1024}')
          BUDGET=819200

          echo "Bundle: $BUNDLE_FILE — ${SIZE_KB} KB (budget: 800 KB)"
          if [ $SIZE_BYTES -gt $BUDGET ]; then
            echo "⚠️  Bundle exceeds budget by $(awk -v s="$SIZE_BYTES" -v b="$BUDGET" 'BEGIN {printf "%.2f", (s-b)/1024}') KB"
          else
            echo "✅ Bundle within budget"
          fi

      - name: Performance budget summary
        if: always()
        run: echo "✅ Performance check complete for ${{ matrix.app }}"

  lighthouse-ci:
    name: Lighthouse CI
    runs-on: ubuntu-latest
    timeout-minutes: 30
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        include:
          - app: webapp
            port: 5173

    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84c5d73d06cf23b74
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: '${{ matrix.app }}/package-lock.json'

      - name: Install dependencies
        run: cd ${{ matrix.app }} && npm ci --legacy-peer-deps

      - name: Build frontend
        run: cd ${{ matrix.app }} && npm run build || true

      - name: Run Lighthouse CI
        continue-on-error: true
        run: |
          cd ${{ matrix.app }}
          npm install -g @lhci/cli@0.10.x
          npm run dev > /tmp/dev.log 2>&1 &
          DEV_PID=$!
          timeout 30 bash -c "until curl -sf http://localhost:${{ matrix.port }} > /dev/null 2>&1; do sleep 0.5; done" || { kill $DEV_PID 2>/dev/null; echo "Server timeout"; exit 0; }
          if [ -f lighthouserc.json ]; then
            BASE_URL=http://localhost:${{ matrix.port }} lhci autorun --config=lighthouserc.json || true
          fi
          kill $DEV_PID 2>/dev/null || true

  dependency-audit:
    name: Dependency Audit
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        app: [webapp]

    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84c5d73d06cf23b74
        with:
          node-version: '22'

      - name: Audit (${{ matrix.app }})
        run: |
          cd ${{ matrix.app }} && npm ci --legacy-peer-deps --ignore-scripts
          npm audit --audit-level=moderate || true

  comment-pr:
    name: Comment PR with Results
    runs-on: ubuntu-latest
    needs: [performance-budget]
    if: github.event_name == 'pull_request' && always()
    continue-on-error: true
    permissions:
      pull-requests: write

    steps:
      - name: Post summary
        run: |
          echo "Performance budget check completed."
          echo "Performance-budget result: ${{ needs.performance-budget.result }}"
