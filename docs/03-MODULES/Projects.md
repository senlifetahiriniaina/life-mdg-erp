# Projects

## Rôle

Le module Projects fournit la gestion de projet complète de l'ERP : projets, tâches, épics, sprints, jalons, dépendances et diagramme de Gantt, équipe et capacité de ressources, automatisations, budget (EVM) et facturation. Il fait partie de la ligne **RH basique** aux côtés de HR, Payroll et Timesheets, et sert de référentiel « projet » central : c'est notamment le module dont dépend Timesheets pour rattacher chaque saisie de temps à un projet/tâche réels.

## Modèles clés

| Modèle | Table DB | Rôle |
|---|---|---|
| `Project` | `prj_projects` | Projet (propriétaire, statut, dates, budget, devise) ; utilise le trait `HelpdeskLinkable` (voir Dépendances). `company_id` ajouté au Chantier 10 (nullable, indexé) — la table n'avait auparavant **aucune** colonne de délimitation multi-entreprise ; alimentée par `ProjectController::store()`, elle scope désormais `index`/`show`/`update`/`destroy` (avec repli gracieux si l'appelant ou le projet n'a pas de `company_id`) et les endpoints `portfolio/*` de `ProjectAdvancedController`. |
| `Task` | `prj_tasks` | Tâche (hiérarchie parent/sous-tâches, épic/sprint/jalon, priorité, points d'histoire) — modèle unique désormais (voir Particularités : `ProjectTask`, un doublon sur la même table, a été supprimé cette session). Colonne `type` ajoutée au Chantier 32.17 (migration `2026_09_21_000001`) : déclarée dans `$fillable`/le docblock depuis toujours mais jamais réellement migrée — chaque écriture réelle de `type` (dont le flux « ajouter une story à un epic ») échouait en SQL. |
| `Milestone` | `prj_milestones` | Jalon de projet, utilisé aussi pour la facturation par jalon (Timesheets) |
| `Epic` | `prj_epics` | Regroupement de tâches au niveau épopée (méthodologie agile) |
| `Sprint` | `prj_sprints` | Sprint agile (burndown, vélocité) |
| `TaskDependency` | `project_task_dependencies` | Dépendances entre tâches, avec détection de cycles |
| `ProjectTeamMember` | `prj_team_members` | Membre d'équipe affecté à un projet |
| `ResourceAllocation` / `ResourceCapacity` | `prj_resource_allocations` / `prj_resource_capacity` | Planification de capacité des ressources humaines sur les projets |
| `AutomationRule` | `prj_automation_rules` | Règle « SI/QUAND/ALORS » déclenchée par les évènements projet |
| `BudgetLine` | `prj_budget_lines` | Ligne budgétaire (CAPEX/OPEX) pour le suivi EVM — table migrée cette session (voir Particularités, elle n'existait pas du tout avant) |
| `ProjectRisk` | `prj_risks` | Risque identifié sur un projet — table migrée cette session |
| `TimeEntry` / `ProjectTimeLog` | `prj_time_entries` / `prj_time_logs` | Suivi de temps propre à Projects (distinct des modèles du module Timesheets) — `ProjectTimeLog` est désormais le seul modèle réel sur `prj_time_logs` (voir Particularités : `TimeLog`, cassé et jamais aligné avec cette table, a été supprimé cette session) |
| `ProjectBilling` (Projects) | `prj_project_billing` | Facturation côté Projects — homonyme du modèle `ProjectBilling` de Timesheets mais table et namespace différents |
| `CustomField` / `CustomFieldValue` | `projects_custom_fields` / `projects_custom_field_values` | Champs personnalisés par entité (task/project/milestone) — Chantier 32.17 : modèle+service réels et testés-en-forme, mais zéro contrôleur/route depuis leur création ; activés pour de vrai (voir Contrôleurs). |
| `SavedView` | `projects_saved_views` | Vue sauvegardée (filtre Kanban/Gantt/Calendrier) |

## Endpoints principaux

Tous sous `auth:sanctum` + `module:Projects` + `role:employee,manager,admin`, montés sous `api/v1` (`Modules/Projects/routes/api.php`) :

| Méthode | Route | Description |
|---|---|---|
| GET/POST/PUT/DELETE | `projects`, `projects/{project}` | CRUD projet |
| GET/POST/PUT/DELETE | `projects/{project}/tasks`, `tasks/{task}` | CRUD tâches scopées au projet. Chantier 32.17 : `index()`/`store()` ne lisaient jamais le `{project}` de la route (seulement un `project_id` séparé en query/corps) — cassait en pratique le seul vrai appelant, `Epics/Index.vue`'s « ajouter une story » (422 systématique) ; corrigé pour utiliser la route comme unique source, avec vérification de société. |
| GET/POST/DELETE | `tasks/{task}/lock` | Verrouillage optimiste de tâche (Chantier 32.17, `TaskConflictResolutionService` activé — voir Particularités) |
| GET/POST/PUT/DELETE | `projects/{project}/milestones`, `projects/{project}/epics`, `projects/{project}/sprints` | CRUD jalons / épics / sprints |
| GET | `projects/{project}/sprints/{sprint}/burndown`, `projects/{project}/velocity`, `projects/{project}/backlog` | Rapports agile |
| POST | `projects/{project}/sprints/{sprint}/start\|complete` | Cycle de vie du sprint |
| GET | `projects/{project}/views/kanban\|calendar\|gantt` | Vues alternatives du projet |
| GET | `projects/{project}/gantt` | Gantt avec dépendances et chemin critique |
| POST/DELETE/PATCH | `projects/tasks/{task}/dependencies`, `projects/tasks/{task}/gantt` | Gestion des dépendances et replanification |
| GET | `projects/{project}/report`, `projects/{project}/report/pdf`, `projects/{project}/report/excel` | Rapport projet (JSON/PDF/Excel — export Excel multi-onglets ajouté au Chantier 32.17, fermant le gap documenté au Chantier 29) |
| GET/POST/DELETE | `projects/{project}/team`, `projects/{project}/time-logs` | Équipe et journal de temps du projet |
| GET/POST/PUT/DELETE | `projects/{project}/members` | Membres du projet |
| GET/POST/PUT/DELETE | `projects/{project}/automations` | Règles d'automatisation |
| GET/POST/PUT/DELETE | `projects/time-entries`, `projects/{project}/tasks/{task}/time-entries` | Suivi de temps (démarrage/arrêt/facturation) |
| GET/POST | `projects/{project}/billing`, `projects/{project}/billing/setup\|mark-billed` | Facturation projet |
| GET/POST/PUT/DELETE | `allocations`, `allocations/{allocation}` | Allocations de ressources — Chantier 32.17 : zéro délimitation par société sur les 5 méthodes CRUD depuis toujours (déjà signalé au Chantier 19 Lot 2 comme « question de design », en réalité un vrai IDOR puisque `project_id` est obligatoire à la création) ; scopé via la société du projet lié, même contrat 404-not-403 que le reste du module. |
| GET/POST/PUT/DELETE | `custom-fields`, `custom-fields/values` | Définitions de champs personnalisés (partagées) et leurs valeurs par entité (scopées par société) — Chantier 32.17, `CustomFieldController` nouveau (voir Particularités) |
| GET | `capacity/utilization`, `capacity/users/{user}/availability`, `capacity/check-overallocation`, `projects/{project}/demand` | Rapports de capacité |
| POST | `capacity/suggest`, `capacity/users/{user}/leave` | Suggestion d'allocation, saisie de congé |
| POST | `projects/ai/estimate-task`, `identify-risks`, `status-report`, `generate-tasks`, `suggest-prioritization` | IA projet (`throttle:expensive`) |
| POST | `v1/projects/ai/assist` | Guidance IA contextuelle (AI Assisted First) |
| GET | `v1/projects/portfolio/kpis\|timeline\|resources`, `v1/projects/{id}/budget\|kpis\|risks` | Budget/KPI/risque avancés (`ProjectAdvancedController`, Phase 49) — sous `module:Projects, role:employee,manager,admin`. Chantier 10 : `portfolio/kpis` filtrait sur `prj_projects.tenant_id`, une colonne jamais migrée — 500 garanti sur chaque appel réel ; `portfolio/timeline`/`portfolio/resources` appelaient `GanttService::getTimelineOverview()`/`getResourceHeatmap()`, deux méthodes qui n'existaient pas du tout — `Error` fatale garantie sur chaque appel. Colonne `company_id` ajoutée à `prj_projects` (migration `2026_09_05_000001`), les trois méthodes construites pour de vrai sur `GanttService`, et la source de tenant corrigée de `$user->tenant_id` (colonne fantôme) vers `$user->company_id`. |
| GET/POST/PUT/DELETE | `projects/time-entries`, `projects/{project}/tasks/{task}/time-entries` | Suivi de temps par tâche, réécrit cette session sur `ProjectTimeLog` (voir Particularités) |

Routes web (`Modules/Projects/routes/web.php`, `auth`+`module:Projects`) : `projects`, `projects/time-report`, `projects/roadmap`, `projects/{project}`, `projects/{project}/{calendar,gantt,kanban,automation,epics,sprints}` — 7 pages réelles ajoutées cette session (voir Particularités, elles n'avaient auparavant aucune route).

## Contrôleurs

API (`Modules/Projects/app/Http/Controllers/Api/`) : `ProjectController` (Chantier 10 : `index`/`show`/`update`/`destroy` scopés par `company_id` du projet, avec repli gracieux si l'appelant ou le projet n'a pas encore de `company_id` réel — voir Particularités), `TaskController` (même scoping, via le `company_id` du projet parent ; Chantier 32.17 — `index()`/`store()` typent désormais `Project $project` sur la route et l'utilisent comme seule source de `project_id`, plus `lock()`/`unlock()`/`lockInfo()` activant `TaskConflictResolutionService`), `MilestoneController`, `EpicController`, `SprintController`, `GanttController` (Chantier 32.17 : `addDependency()` rejette désormais une dépendance vers une tâche d'un autre projet — un vrai IDOR cross-tenant confirmé empiriquement), `ProjectViewsController` (Chantier 32.17 : `gantt()`'s champ `dependencies` par tâche lisait la colonne JSON `Task.dependencies`, jamais écrite par aucun code réel, au lieu de la vraie relation `TaskDependency` — corrigé), `ProjectReportController` (Chantier 32.17 : `excel()` ajouté), `ProjectTeamController` (Chantier 32.17 : `storeTimeLog()` vérifie désormais que `task_id` appartient bien au projet), `ProjectMemberController`, `AutomationController`, `ResourceCapacityController` (Chantier 32.17 : scoping société ajouté sur les 5 méthodes CRUD d'allocations), `TimeEntryController` (réécrit cette session sur `ProjectTimeLog` — voir Particularités ; Chantier 32.17 : vérifie désormais que la tâche appartient au projet sur les 5 méthodes), `TimeTrackingController` (Chantier 32.17 : `store()`/`start()` vérifient que `task_id` appartient au `project_id` fourni), `CustomFieldController` (Chantier 32.17, nouveau — voir Particularités), `ProjectAdvancedController` (Phase 49 : budget/KPI/risques/portefeuille — ne conserve plus que `budget`/`kpis`/`risks`/`portfolioKpis`/`portfolioTimeline`/`portfolioResources`, les seules méthodes réellement routées ; `index`/`store`/`show`/`gantt`/`storeTask`/`updateTask` étaient du code mort inatteignable — `storeTask`/`updateTask` référençaient même `ProjectTask`, un modèle supprimé au Chantier 8.4 — et `store()` contenait en plus une vulnérabilité par en-tête `X-Tenant-ID` contrôlable par le client ; supprimés au Chantier 10 plutôt que corrigés, la vraie voie CRUD restant `ProjectController`/`TaskController`/`GanttController`), `ProjectsAIController`, `ProjectsAiAssistController`.

Web (`Modules/Projects/app/Http/Controllers/Web/`) : **`ProjectWebController`** (index/show/calendar/gantt/kanban/automation/epics/sprints/roadmap), **`ProjectTimeReportController`** (rapport de temps global, réécrit cette session sur `TimeTrackingController`). Un `ProjectsController` scaffold mort (zéro route + vue Blade) et 3 policies inatteignables (`Modules\Projects\Policies\{Project,Task,Milestone}Policy` — les vraies policies actives sont `App\Policies\{Project,Task}Policy`) ont été supprimés.

## Vues (Vue/Inertia)

Toutes les pages Projects actives vivent désormais à la **racine** (`resources/js/Pages/Projects/`) — le module `Modules/Projects/resources/js/Pages/` ne contient plus aucune page après suppression du doublon `TimeReport/Index.vue` masqué cette session : `Index.vue`, `Show.vue`, `Calendar.vue`, `Gantt.vue`, `Kanban.vue`, `Roadmap.vue`, `Automation/Index.vue`, `Epics/Index.vue`, `Sprints/Index.vue`, `TimeReport/Index.vue`. Les 7 premières (`Automation`/`Calendar`/`Epics`/`Gantt`/`Kanban`/`Roadmap`/`Sprints`) étaient réelles et entièrement construites mais **n'avaient aucune route web** avant cette session ; `TimeReport/Index.vue` appelait un endpoint `time-report-global` qui n'existait pas — bâti sur `TimeTrackingController`, avec agrégation réelle par membre/projet/tâche et montant facturable.

**Chantier 32.17 — assistance IA contextuelle câblée sur les 10 pages** : `AiContextualAssistantService::supportedModules()` n'avait aucune entrée `'Projects'` (repli silencieux vers une guidance vide sur chaque appel réel), et seules 2 des 10 pages (`Index.vue`, `Cascade/Index.vue` — en réalité `Index.vue`/`Show.vue` de ce module) appelaient `useAiAssistant()`. Les 10 actions réelles (`view_dashboard`, `view_kanban`, `view_gantt`, `view_calendar`, `view_automation`, `view_epics`, `view_sprints`, `view_roadmap`, `view_time_report`, `view_project`) sont désormais enregistrées avec un vrai texte de repli fr+en, et les 10 pages appellent toutes `useAiAssistant('Projects', '<action>')` + affichent `<AIAssistantPanel>`.

## Services

- **`AutomationService`** — évalue les règles d'automatisation actives (`AutomationRule`) correspondant à un déclencheur donné. Chantier 32.17 : confirmé sans aucun producteur réel jusqu'ici (zéro appelant hors ses propres tests) — activé pour de vrai via `TaskController::store()`/`update()`, qui déclenchent désormais `task_created`/`task_status_changed`/`task_assigned` sur les vraies transitions. Le déclencheur `comment_added` reste réel mais toujours sans producteur, Projects n'ayant pas de concept de commentaire de tâche propre.
- **`CustomFieldService`** — définitions de champs personnalisés et leurs valeurs par projet. Chantier 32.17 : réel et testé mais **sans aucun contrôleur/route** avant cette session (couche 9, « à activer ») — nouveau `CustomFieldController` (`custom-fields`, `custom-fields/values`) construit dessus.
- **`DependencyCycleDetectionService`** — détection de cycles dans le graphe de dépendances entre tâches (parcours en profondeur, profondeur max 10 niveaux) avant d'autoriser l'ajout d'une dépendance.
- **`GanttService`** — construction des données du diagramme de Gantt (dates, dépendances, chemin critique).
- **`ProjectBudgetService`** — suivi budgétaire EVM (SPI/CPI/EAC), mapping des comptes OHADA (CAPEX Cl.2, OPEX Cl.6, revenus Cl.7061), courbe en S, alertes budgétaires ; montants exprimés en XOF.
- **`ProjectKpiService`** — KPI projet et portefeuille, intégration Strategy First, tendances de vélocité, score de santé projet. Chantier 32.17 : `getVelocityTrend()`/`getTeamUtilization()` interrogeaient la table `ts_timesheets`, qui n'a jamais existé dans ce dépôt (confirmé via `Schema::hasTable`) — un bug jumeau de celui déjà corrigé sur `GanttService::getResourceHeatmap()` au Chantier 19 Lot 2, manqué à l'époque sur ce fichier-ci. Corrigé pour interroger la vraie table `timesheet_entries`/colonnes `entry_date`/`hours_worked` du module Timesheets.
- **`ProjectReportService`** — génération des rapports projet (JSON/PDF). Chantier 32.17 : nouvelle méthode `taskRows()` alimentant le nouvel export Excel — voir Particularités.
- **`ProjectTeamService`** — ajout/retrait de membres d'équipe et leur rôle.
- **`ResourceCapacityService`** — allocation de ressources, calcul de disponibilité et de sur-allocation. Chantier 32.17 : le CRUD `allocations` de `ResourceCapacityController` (qui consomme ce service) n'avait aucun scoping société malgré `project_id` étant toujours requis à la création — re-tranché comme un vrai bug corrigible (pas une « question de conception » comme documenté au Chantier 19 Lot 2) et corrigé, voir Particularités.
- **`SprintService`** — cycle de vie du sprint (démarrage, complétion).
- **`TaskConflictResolutionService`** — verrouillage pessimiste des tâches en édition concurrente et résolution « dernier écrit gagne ». Chantier 32.17 : réel, testé (16/16 déjà avant cette session), mais **sans aucune route** — activé pour de vrai via `TaskController::lock()`/`unlock()`/`lockInfo()`.
- **`TimeTrackingDeduplicationService`** — déduplique les saisies de temps d'un utilisateur/date et documente explicitement le module **Timesheets** comme source de vérité unique pour le suivi de temps. Chantier 32.17 : re-confirmé cassé (compare des noms de colonnes qui ne correspondent pas au vrai schéma `timesheet_entries` de Timesheets) **et** toujours sans aucun appelant réel dans Projects — délibérément **laissé en l'état, non corrigé**, conformément à l'instruction explicite de ce chantier de ne pas trancher unilatéralement la réconciliation cross-module Projects/Timesheets pendant qu'un agent parallèle auditait Timesheets ; voir Particularités.
- **`TimeTrackingService`** — démarrage/arrêt de minuteur et création de `TimeEntry` côté Projects.
- **`Services\AI\ProjectsAIService`** — appelle `Modules\Core\Services\AI\AIService` pour l'estimation de tâches, l'identification de risques, la génération de rapports de statut, la génération de tâches et la suggestion de priorisation.

## Exports

**Chantier 32.17 (nouveau)** : `GET /api/v1/projects/{project}/report/excel` (`ProjectReportController::excel()`) — un vrai classeur Excel (`Modules\Projects\Exports\ProjectReportExport`, patron `WithMultipleSheets` déjà établi par `Modules\Strategy\Exports\StrategyExecutiveReportExport`) à 3 onglets (Tâches / Heures équipe / Jalons), construit sur les mêmes données déjà réelles que le rapport PDF existant (`ProjectReportService`), pas de nouveau calcul. Bouton « Export Excel » ajouté à `Components/Projects/ExportButton.vue`, aux côtés du bouton PDF déjà existant — ferme le gap documenté au Chantier 29 (« zéro export Excel dans ce module malgré un export PDF réel »).

## Permissions RBAC

Permissions dédiées sous le préfixe `projects.*` dans `database/seeders/RolesAndPermissionsSeeder.php` : ressources `project` et `task`, actions `view-any|view|create|update|delete` (plus `approve`, `export`, `archive` référencées par `ProjectPolicy` mais non déclarées dans la liste `MODULES` du seeder — inchangé cette session). Le rôle `project-manager` reçoit l'ensemble des permissions `projects.*` ainsi que `timesheets.*` et un accès en lecture aux employés HR.

Correction RBAC de cette session : le groupe de routes Phase 49 (`portfolio/*`, `{id}/budget\|kpis\|risks`) n'avait **aucune** gating `module:`/`role:` — n'importe quel utilisateur authentifié de n'importe quel tenant pouvait lire le budget/KPI/risque de n'importe quel projet. Corrigé avec `module:Projects, role:employee,manager,admin`, identique au reste du module. Ses tables (`prj_budget_lines`/`prj_expense_ledger`/`prj_risks`) n'avaient elles-mêmes aucune migration — chaque appel réel échouait de toute façon (« table not found ») avant que le trou RBAC ne soit même exploitable ; corrigé par la même migration.

## Dépendances avec d'autres modules

- **Utilise `Modules\Helpdesk`** : `Project` utilise le trait `Modules\Helpdesk\Traits\HelpdeskLinkable`, ce qui permet à tout projet de créer (`raiseTicket()`) et lister (`tickets`) ses propres tickets Helpdesk.
- **Utilise `Modules\Core`** : trait `RecordsActivity` sur `Project`, et `Modules\Core\Services\AI\AIService` dans `ProjectsAIService`.
- **Utilise `Modules\AI`** : `ProjectsAiAssistController` appelle `Modules\AI\Services\AiContextualAssistantService`.
- **Dépendance côté Timesheets** : Projects ne dépend d'aucune classe du module Timesheets (aucun `use Modules\Timesheets\...` trouvé dans le code de Projects), mais c'est l'inverse qui est vrai — voir `docs/03-MODULES/Timesheets.md`. `TimeTrackingDeduplicationService` documente ce lien logique sans introduire de dépendance de code.

## Particularités du périmètre life-mdg-erp

- **La fonctionnalité wiki de projet a été retirée**, comme documenté dans `CLAUDE.md` : aucune trace de `ProjectWikiService`, `ProjectWikiController`, ni d'aucune référence au module `Notes` (`Modules\Notes\Models\Note`) n'existe dans le code actuel de `Modules/Projects` — confirmé par une recherche exhaustive des chaînes `Notes`, `Wiki`/`wiki` dans le module, qui ne retourne aucun résultat. C'était une fonctionnalité annexe autonome, retirée plutôt que transformée en stub, car elle dépendait du module Notes exclu du périmètre life-mdg-erp.
- **Trois piles de suivi de temps qui se chevauchaient ont été réduites cette session** : `Task`/`ProjectTask` partageaient `prj_tasks` (le second n'ajoutait que des accesseurs EVM/Gantt) et `TimeLog`/`ProjectTimeLog` partageaient `prj_time_logs`, mais `TimeLog` avait un `$fillable` qui ne correspondait jamais réellement à cette table. `ProjectTask` et `TimeLog` (+ leurs factories) ont été supprimés ; `TimeEntryController` (endpoint `projects/{project}/tasks/{task}/time-entries`, déjà routé) a été réécrit sur `ProjectTimeLog`, le modèle qui correspond réellement au schéma, avec un adaptateur convertissant la saisie manuelle simple (heures + date) vers la forme « minuteur » réellement stockée par ce modèle.
- **Référence orpheline non déclenchée trouvée pendant cette vérification** (non corrigée — hors du périmètre « documentation » de cette passe, signalée ici plutôt que masquée) : `Task::timeLogs()` (`Modules/Projects/app/Models/Task.php`) référence toujours `TimeLog::class` — la classe supprimée ci-dessus — sans qu'aucune classe de ce nom n'existe plus dans `Modules\Projects\Models`. Aucun appelant de cette relation n'a été trouvé dans le dépôt (`$task->timeLogs`/`timeLogs()` : zéro résultat hors de sa propre définition), donc ce n'est pas une régression active, mais le premier appel réel lèverait une erreur de classe introuvable.
- **Chantier 10 — `ProjectAdvancedController` nettoyé, `prj_projects` doté d'une vraie colonne de tenant, deux méthodes `GanttService` construites pour de vrai** : `index()`/`store()`/`show()`/`gantt()`/`storeTask()`/`updateTask()` étaient déjà documentés comme délibérément non routés (collision avec `ProjectController`/`TaskController`/`GanttController`), mais restaient du code mort dangereux : `store()` acceptait un en-tête `X-Tenant-ID` contrôlable par le client comme repli pour `tenant_id` (même vulnérabilité que celle déjà corrigée pour Setup au Chantier 8.5sv), `index()` acceptait un filtre `tenant_id` entièrement fourni par le client, et `storeTask()`/`updateTask()` référençaient `ProjectTask`, un modèle supprimé au Chantier 8.4 — une `Error` de classe introuvable garantie au premier appel si jamais quelqu'un ré-activait ces routes sans ré-auditer. Les six méthodes ont été supprimées plutôt que corrigées. `portfolio/kpis`/`portfolio/timeline`/`portfolio/resources` (les trois endpoints réellement routés) étaient en revanche **activement cassés** : `portfolioKpis()` filtrait sur `prj_projects.tenant_id`, une colonne jamais migrée (500 garanti à chaque appel réel, pas une éventualité) ; `portfolioTimeline()`/`portfolioResources()` appelaient `GanttService::getTimelineOverview()`/`getResourceHeatmap()`, deux méthodes qui n'existaient pas du tout sur cette classe (`Error` fatale garantie). Corrigés en ajoutant `company_id` à `prj_projects` (migration `2026_09_05_000001_add_company_id_to_prj_projects.php`) et en construisant réellement les deux méthodes manquantes sur `GanttService` (agrégation par entreprise sur les projets actifs/tâches/`prj_team_members`, avec repli défensif sur les heures de `ts_timesheets` si la table est absente).
- **`ProjectController`/`TaskController` scopés par `company_id`** : n'avaient jusqu'ici strictement aucune notion de délimitation multi-entreprise — `index()` listait tous les projets de toutes les entreprises, `show()`/`update()`/`destroy()` n'avaient aucune vérification de propriété au-delà de `BaseErpPolicy` (qui autorise volontairement toute lecture « dans le tenant », en supposant qu'un autre mécanisme fait déjà le filtrage — ce qui n'était pas le cas ici). Scopés avec un repli gracieux (`when()` : le filtre ne s'applique que si l'utilisateur appelant a un `company_id` réel) pour ne pas casser les données/tests existants créés avant l'ajout de la colonne ; `TaskController` applique le même filtre via la relation `project`.
- **Chantier 32.17 — audit approfondi en 14 couches, deuxième passe après Chantier 19 Lot 2** : re-confirmé que `ScopesToProjectCompany` (le trait cross-tenant du Chantier 19 Lot 2) est toujours correctement appliqué sur tous les contrôleurs déjà couverts — aucune régression — mais 6 endpoints ajoutés ou jamais audités depuis portaient encore un vrai IDOR/bug de couche 4-6, tous corrigés :
  - **`GanttController::addDependency()`** — acceptait `depends_on_task_id` sans jamais vérifier qu'elle appartenait au même projet que la tâche dépendante ; confirmé empiriquement qu'une dépendance vers une tâche d'un **autre projet/société** était acceptée, ce qui aurait permis de décaler silencieusement les dates d'une tâche étrangère via la propagation de `updateDates()`. Corrigé (422 si `project_id` diffère, avant le contrôle de cycle).
  - **`ProjectViewsController::gantt()`** — le champ `dependencies` par tâche lisait `Task.dependencies`, une colonne JSON **jamais écrite par aucun code réel** (le vrai stockage est la relation `TaskDependency`), à cause d'une collision de nommage Eloquent (l'attribut casté `$casts` gagne toujours sur une relation de même nom). Le diagramme de Gantt affichait donc systématiquement aucune dépendance. Corrigé (eager-load `dependencies`, lecture via `getRelation('dependencies')`).
  - **`ResourceCapacityController`** — le CRUD `allocations` (`index`/`store`/`show`/`update`/`destroy`) n'avait **aucun** scoping société, alors que `project_id` est toujours requis à la création — ré-audité comme un vrai bug corrigible (le Chantier 19 Lot 2 l'avait qualifié de « question de conception », à tort : la donnée nécessaire au scoping existe déjà). Corrigé avec le même patron `assertSameCompany*` déjà établi par le trait.
  - **`TimeEntryController`** (les 5 méthodes) et **`ProjectTeamController::storeTimeLog()`** et **`TimeTrackingController::store()`/`start()`** — aucun ne vérifiait que le `task_id` fourni appartenait réellement au `project`/`project_id` de la requête ; une tâche d'un autre projet (potentiellement d'une autre société) pouvait recevoir des heures via ces 3 points d'entrée. Corrigés avec des vérifications explicites (`task->project_id !== project->id` → 404/422 selon le contrôleur).
  - **`ProjectKpiService::getVelocityTrend()`/`getTeamUtilization()`** — interrogeaient la table `ts_timesheets`, qui n'a jamais existé dans ce dépôt (confirmé via `Schema::hasTable`) — le même bug déjà corrigé sur `GanttService::getResourceHeatmap()` au Chantier 19 Lot 2, manqué à l'époque sur ce fichier. Corrigés pour interroger la vraie table `timesheet_entries` du module Timesheets (`entry_date`/`hours_worked`, entrées approuvées uniquement).
  - **Bug bloquant trouvé en écrivant les tests, pas en relisant le code** : `TaskController::index()`/`store()` étaient routés uniquement sous `projects/{project}/tasks` mais n'utilisaient jamais le `Project $project` lié à la route — filtraient sur un `?project_id=`/body `project_id` séparé que l'appelant devait fournir en plus. Le vrai appel réel (`Epics/Index.vue`'s `addStory()`, qui poste sans `project_id` dans le corps, comptant sur l'URL) échouait à 422 sur chaque clic « Ajouter une story ». Corrigé en typant `Project $project` sur les deux routes et en l'utilisant comme seule source de vérité. Ce correctif a lui-même révélé un second bug de schéma : `Task.$fillable` déclare `type` mais `prj_tasks` n'a jamais eu cette colonne (migration additive nullable ajoutée).
  - **Couche 9 (fake/dead) — 3 décisions « à activer », zéro suppression** : `CustomFieldService` (réel, testé, zéro contrôleur/route) → nouveau `CustomFieldController` construit dessus (`custom-fields`, `custom-fields/values`, avec vérification de propriété société via le projet parent de l'entité ciblée). `AutomationService::evaluate()` (réel, zéro producteur) → branché sur `TaskController::store()`/`update()` pour les 3 déclencheurs réellement produisibles par ce module (`task_created`/`task_status_changed`/`task_assigned`) ; `comment_added` reste un déclencheur réel sans producteur, Projects n'ayant pas de concept de commentaire de tâche. `TaskConflictResolutionService` (déjà confirmé réel/testé au Chantier 9, jamais routé) → nouveaux `TaskController::lock()`/`unlock()`/`lockInfo()`.
  - **`TimeTrackingDeduplicationService` — confirmé toujours cassé et sans producteur, délibérément laissé en l'état** : compare des noms de colonnes qui ne correspondent pas au vrai schéma `timesheet_entries` de Timesheets, et zéro appelant dans Projects. Conformément à l'instruction explicite de ce chantier de ne pas trancher unilatéralement la réconciliation cross-module Projects/Timesheets pendant qu'un agent parallèle auditait Timesheets, **non corrigé et non supprimé** — flagué pour une décision conjointe lors d'un futur audit Timesheets.
  - **Couche 10 (relationnel), constat documenté, non corrigé** : 7 tables ont une relation Eloquent `belongsTo()` réelle côté modèle mais **aucune contrainte FK au niveau base** (confirmé via `PRAGMA foreign_key_list`) : `prj_team_members`, `prj_time_entries`, `prj_epics`, `prj_sprints`, `prj_automation_rules`, `prj_resource_allocations`, `project_task_dependencies`. Aucune preuve trouvée que cela cause un vrai bug aujourd'hui (aucune ligne orpheline observée) — mais une intégrité référentielle absente au niveau base, pas seulement applicative. Ajouter les contraintes FK a été jugé comme un changement de schéma plus risqué (verrous de migration sur des tables potentiellement volumineuses en production) que le périmètre d'un audit-et-correction ; laissé comme un gap documenté plutôt que corrigé sur un coup de tête.
  - **Couche 13 (IA)** : voir la section Vues ci-dessus — `'Projects'` absent de `AiContextualAssistantService::supportedModules()`, corrigé avec 10 actions réelles fr+en (`Modules\AI\app\Services\AiContextualAssistantService.php`, seul fichier hors `Modules/Projects/**` touché par ce chantier), et les 10 pages Vue réelles câblées.
  - **Couche 14 (export)** : voir la section Exports ci-dessus — nouvel export Excel construit sur les données déjà réelles du rapport PDF existant, fermant le gap documenté au Chantier 29.
  - **Vérifié** : `vendor/bin/pest Modules/Projects --parallel` → **236 passés, 1 skip préexistant** (220 de référence Chantier 19 Lot 2 + 16 nouveaux tests, zéro régression) ; `php -l` propre sur tous les fichiers touchés/créés ; `php artisan route:list` confirme les nouvelles routes sans collision ; `npm run type-check` propre sur toutes les pages Projects (4 erreurs préexistantes non liées, `HR/Employees/Show.vue`, confirmées appartenir à un agent parallèle concurrent) ; `git status --porcelain=v1` confirme que seuls `Modules/Projects/**`, `Modules/AI/app/Services/AiContextualAssistantService.php`, et les pages Vue racine sous `resources/js/Pages/Projects/**`/`resources/js/Components/Projects/**` (le vrai frontend de ce module, en dehors de `Modules/Projects/**` par convention établie de longue date — voir la section Vues) ont été touchés.
